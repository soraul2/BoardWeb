<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>게시판 - Community</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Apple Style CSS */
        :root {
            --apple-bg: #f5f5f7;
            --apple-card: #ffffff;
            --apple-text: #1d1d1f;
            --apple-blue: #0071e3;
            --apple-gray: #86868b;
            --apple-border: #d2d2d7;
        }
        body {
            background-color: var(--apple-bg);
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            color: var(--apple-text);
        }

        /* 헤더 */
        header {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(20px);
            padding: 15px 0;
            text-align: center;
            position: sticky;
            top: 0;
            z-index: 100;
            border-bottom: 1px solid var(--apple-border);
        }
        header a {
            color: var(--apple-text);
            text-decoration: none;
            margin: 0 15px;
            font-size: 14px;
            font-weight: 500;
            transition: color 0.2s;
        }
        header a:hover { color: var(--apple-blue); }
        .nav-active { color: var(--apple-blue) !important; font-weight: 600; }

        /* 메인 컨테이너 */
        .container {
            max-width: 980px;
            margin: 60px auto;
            padding: 0 20px;
        }

        /* 상단 영역 (제목 + 검색 + 버튼) */
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        .page-title {
            font-size: 32px;
            font-weight: 700;
        }

        /* 검색창 스타일 */
        .search-form {
            display: flex;
            gap: 8px;
        }
        .search-input {
            border: 1px solid var(--apple-border);
            border-radius: 12px;
            padding: 8px 15px;
            font-size: 14px;
            background: #fff;
            outline: none;
            width: 200px;
            transition: all 0.2s;
        }
        .search-input:focus {
            border-color: var(--apple-blue);
            box-shadow: 0 0 0 3px rgba(0, 113, 227, 0.1);
        }
        .btn-search {
            background: #e5e5e7;
            border: none;
            border-radius: 12px;
            padding: 0 15px;
            font-size: 13px;
            font-weight: 600;
            color: var(--apple-text);
        }

        /* 글쓰기 버튼 */
        .btn-write {
            background-color: var(--apple-blue);
            color: white;
            padding: 10px 20px;
            border-radius: 20px;
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            transition: background-color 0.2s;
            margin-left: 10px;
        }
        .btn-write:hover { background-color: #0077ed; color: white; }

        /* 게시판 테이블 */
        .board-card {
            background: var(--apple-card);
            border-radius: 18px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
            overflow: hidden;
            margin-bottom: 30px;
        }

        table { width: 100%; border-collapse: collapse; }
        th {
            background-color: #fafafa;
            color: var(--apple-gray);
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            padding: 15px 20px;
            text-align: left;
            border-bottom: 1px solid #eeeeee;
        }
        td {
            padding: 18px 20px;
            border-bottom: 1px solid #eeeeee;
            font-size: 15px;
            color: var(--apple-text);
        }
        tr:last-child td { border-bottom: none; }
        tbody tr { transition: background-color 0.2s; cursor: pointer; }
        tbody tr:hover { background-color: #f9f9f9; }
        .post-link { color: inherit; text-decoration: none; display: block; }

        /* 데이터 없음 & 페이지네이션 */
        .empty-state {
            text-align: center;
            padding: 60px 0;
            color: var(--apple-gray);
        }

        .pagination-area {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px;
            margin-top: 20px;
        }
        .page-btn {
            text-decoration: none;
            color: var(--apple-text);
            background: #fff;
            padding: 8px 16px;
            border-radius: 12px;
            font-size: 14px;
            font-weight: 500;
            box-shadow: 0 2px 5px rgba(0,0,0,0.05);
            transition: all 0.2s;
        }
        .page-btn:hover { background: #f0f0f0; transform: translateY(-1px); }
        .page-info { font-size: 14px; color: var(--apple-gray); }

    </style>
</head>
<body>

<header>
    <nav>
        <a href="/">Home</a>
        <a href="/board/list" class="nav-active">Board</a>

        {{#isLogin}}
            <a href="/user/myinfo">My Info</a>
            <span style="font-size: 12px; color: #86868b; margin-left: 10px;">{{username}}님</span>
            <a href="/logout" style="color: #ff3b30;">Logout</a>
        {{/isLogin}}

        {{^isLogin}}
            <a href="/login">Login</a>
            <a href="/join">Join</a>
        {{/isLogin}}
    </nav>
</header>

<div class="container">

    <div class="page-header">
        <h1 class="page-title">Community</h1>

        <div style="display: flex; align-items: center;">
            <form action="/board/list" method="get" class="search-form">
                <input type="text" name="keyword" class="search-input" placeholder="Search..." value="{{#keyword}}{{keyword}}{{/keyword}}">
                <button type="submit" class="btn-search">검색</button>
            </form>

            {{#isLogin}}
                <a href="/board/write" class="btn-write">New Post +</a>
            {{/isLogin}}
        </div>
    </div>
    {{#error}}
        <script>
            alert("{{error}}");
            // 알림창 닫으면 깔끔하게 URL에서 에러 파라미터 지우기 (선택사항)
            history.replaceState({}, null, location.pathname);
        </script>
    {{/error}}
    <div class="board-card">
        <table>
            <thead>
            <tr>
                <th style="width: 10%;">NO</th>
                <th style="width: 55%;">SUBJECT</th>
                <th style="width: 15%;">WRITER</th>
                <th style="width: 20%;">DATE</th>
            </tr>
            </thead>
            <tbody>
            {{#boardList.content}}
                <tr onclick="location.href='/board/detail/{{num}}'">
                    <td>{{num}}</td>
                    <td><span class="post-link">{{title}}</span></td>
                    <td>{{nickname}}</td> <td>{{date}}</td>
                </tr>
            {{/boardList.content}}

            {{#boardList.empty}}
                <tr>
                    <td colspan="4" class="empty-state">
                        <span style="font-size: 24px; display: block; margin-bottom: 10px;">📭</span>
                        등록된 게시글이 없습니다.
                    </td>
                </tr>
            {{/boardList.empty}}
            </tbody>
        </table>
    </div>

    <div class="pagination-area">
        {{^boardList.first}}
            <a href="/board/list?page={{previous}}{{#keyword}}&keyword={{keyword}}{{/keyword}}" class="page-btn">← Prev</a>
        {{/boardList.first}}

        <span class="page-info">Page {{current}} / {{boardList.totalPages}}</span>

        {{^boardList.last}}
            <a href="/board/list?page={{next}}{{#keyword}}&keyword={{keyword}}{{/keyword}}" class="page-btn">Next →</a>
        {{/boardList.last}}
    </div>

</div>

</body>
</html>